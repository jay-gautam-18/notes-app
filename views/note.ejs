<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Submit a Note</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Slide-down and fade animation */
    @keyframes slideDownFade {
      0% { transform: translateY(-30px); opacity: 0; }
      100% { transform: translateY(0); opacity: 1; }
    }
    .slide-down-fade {
      animation: slideDownFade 0.7s ease-out forwards;
    }

    /* Glass effect */
    .glass {
      background: rgba(255, 255, 255, 0.6);
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }
  </style>
</head>
<body class="bg-gradient-to-br from-orange-400 via-yellow-200 to-white min-h-screen flex items-center justify-center text-gray-800">

  <!-- Navbar -->
  <nav class="absolute top-3 w-[90vw] rounded-xl bg-gradient-to-r from-red-600 via-orange-600 to-yellow-500 p-5 shadow-lg rounded-b-2xl mb-12">
    <div class="container mx-auto flex justify-between items-center">
      <a href="/notes" class="text-white text-3xl font-extrabold tracking-wider transform hover:scale-110 transition-transform duration-300">
        Submit a Note
      </a>
      <ul class="flex space-x-8 text-white font-semibold">
        <li><a href="/" class="hover:text-purple-200 transition duration-300">Home</a></li>
        <li><a href="/notes" class="hover:text-purple-200 transition duration-300">My Notes</a></li>
      </ul>
    </div>
  </nav>

 
        
        <!-- Title Input -->
        <div class="relative">
<!-- Submission Form Section -->
<section class="container mx-auto mt-20 p-6 slide-down-fade">
  <div class="glass p-10 rounded-3xl shadow-2xl max-w-lg mx-auto text-center">
    <h2 class="text-5xl font-extrabold text-orange-600 mb-8 animate-pulse tracking-wide">
      Create a New Note
    </h2>
    
    <!-- Form Start -->
    <form action="/notes" method="POST" class="space-y-6">
      
      <!-- Title Input -->
      <div class="relative">
        <label for="title" class="block text-gray-700 text-sm font-bold mb-2">Note Title</label>
        <input type="text" id="title" name="title" required
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
          placeholder="Enter note title">
      </div>
  
      <!-- Description Input -->
      <div class="relative">
        <label for="content" class="block text-gray-700 text-sm font-bold mb-2">Note Content</label>
        <textarea id="content" name="content" rows="8" required
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500 resize-none"
          placeholder="Enter note content"></textarea>
      </div>
  
      <!-- Submit Button -->
      <div>
        <button type="submit"
          class="w-full bg-yellow-600 text-white py-3 px-6 rounded-md hover:bg-yellow-500 transition-colors duration-300 font-semibold">
          Save Note
        </button>
      </div>
    </form>
    <!-- Form End -->
  </div>

  
</body>
</html>